import { ReactNode } from 'react';
interface Props {
    children: ReactNode;
    header: ReactNode;
}
export default function HeaderLayout({ children, header }: Props): import("react").JSX.Element;
export {};
