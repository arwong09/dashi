import { ReactNode } from 'react';
interface Props {
    children: ReactNode;
    onClick: () => void;
}
export default function SecondaryButton({ children, onClick }: Props): import("react").JSX.Element;
export {};
