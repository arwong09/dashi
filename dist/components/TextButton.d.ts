import { ReactNode } from 'react';
interface Props {
    children: ReactNode;
    href: string;
}
export default function TextButton({ children, href }: Props): import("react").JSX.Element;
export {};
